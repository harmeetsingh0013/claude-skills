# <Product Name>

<Brief overview — 2-4 sentences. What the product is and what problem it
solves. Pull this from the Mini-PRD's problem statement and primary goal;
don't re-explain functional requirements, architecture, or implementation
detail here — that's what the linked documents are for.>

## Documents

| Document | Current Version | |
|----------|------------------|---|
| Mini-PRD | v<X.Y> | [Browse versions](./mini-prd/) |
| Functional Requirements | v<X.Y> | [Browse versions](./functional-requirements/) |
| Non-Functional Requirements | v<X.Y> | [Browse versions](./non-functional-requirements/) |
| Architecture & Design | v<X.Y> | [Browse versions](./architecture-design/) |
| Diagrams | v<X.Y> | [Browse versions](./mermaid-diagrams/) |
<!-- add a Sprints row only if sprints exist -->

Each link goes to that document's folder, where every version
(`v1.0.md`, `v1.1.md`, ...) is available — pick whichever one you need;
this README always points at the folder, not a single locked-in version.

## Diagrams

<One subsection per diagram actually produced by mermaid-js — in the same
order as that stage's index.md. Each diagram is embedded directly so it
renders inline wherever this README is viewed (GitHub, GitLab, and most
markdown viewers render ```mermaid fenced blocks natively).>

### <Diagram Name>

<One-line caption — what it shows, from mermaid-diagrams' own record of
that.>

```mermaid
<the diagram's actual content, copied verbatim from its .mmd file>
```

<repeat per diagram>

---

Generated by the `project-readme` skill. Project ID: `<project-id>`.
